import React from 'react';
import { Container } from '@mui/material';

export class BGGlobal extends React.Component {
  render () {
    return (
      <Container maxWidth="xl">
      </Container>
    );
  }
}