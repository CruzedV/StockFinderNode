{
  "subscribeCandlesRequest": {
    "subscriptionAction": "SUBSCRIPTION_ACTION_UNSUBSCRIBE",
    "instruments": [
      {
        "figi": "Auto Loan Account calculate",
        "interval": "SUBSCRIPTION_INTERVAL_FIVE_MINUTES",
        "instrumentId": "{{ faker.random.uuid }}"
      },
      {
        "figi": "Auto Loan Account calculate",
        "interval": "SUBSCRIPTION_INTERVAL_ONE_MINUTE",
        "instrumentId": "{{ faker.random.uuid }}"
      }
    ],
    "waitingClose": true
  },
  "subscribeOrderBookRequest": {
    "subscriptionAction": "SUBSCRIPTION_ACTION_UNSPECIFIED",
    "instruments": [
      {
        "figi": "Auto Loan Account calculate",
        "depth": 641,
        "instrumentId": "{{ faker.random.uuid }}"
      },
      {
        "figi": "Auto Loan Account calculate",
        "depth": 993,
        "instrumentId": "{{ faker.random.uuid }}"
      }
    ]
  },
  "subscribeTradesRequest": {
    "subscriptionAction": "SUBSCRIPTION_ACTION_SUBSCRIBE",
    "instruments": [
      {
        "figi": "Auto Loan Account calculate",
        "instrumentId": "{{ faker.random.uuid }}"
      },
      {
        "figi": "Auto Loan Account calculate",
        "instrumentId": "{{ faker.random.uuid }}"
      }
    ]
  },
  "subscribeInfoRequest": {
    "subscriptionAction": "SUBSCRIPTION_ACTION_SUBSCRIBE",
    "instruments": [
      {
        "figi": "Auto Loan Account calculate",
        "instrumentId": "{{ faker.random.uuid }}"
      },
      {
        "figi": "Auto Loan Account calculate",
        "instrumentId": "{{ faker.random.uuid }}"
      }
    ]
  },
  "subscribeLastPriceRequest": {
    "subscriptionAction": "SUBSCRIPTION_ACTION_UNSPECIFIED",
    "instruments": [
      {
        "figi": "Auto Loan Account calculate",
        "instrumentId": "{{ faker.random.uuid }}"
      },
      {
        "figi": "Auto Loan Account calculate",
        "instrumentId": "{{ faker.random.uuid }}"
      }
    ]
  }
}